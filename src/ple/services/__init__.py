"""IO and business logic (subprocess, git, jupyter, http, filesystem).

Planned home for service modules extracted from the legacy controller. Pure
Python — must not import PyQt6 — so these stay unit-testable without a display.
"""
