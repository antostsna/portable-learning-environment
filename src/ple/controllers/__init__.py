"""Controllers wire view signals to services and models.

Planned home for the sub-controllers that will replace the monolithic
``controler.Controller`` (lesson, submission, docs, settings, welcome).
"""
